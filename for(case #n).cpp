#include <stdio.h>
int main()
{
	int a,b,i;
	scanf("%d",&i);	
	for(i=1; i<=10; i++)
	{
		while(i<=i)
		{
		scanf("%d %d", &a, &b);
		printf("Case #%d: %d + %d = %d\n",l,a,b,a+b);
		l++;
		}
	}
	return 0;
}
