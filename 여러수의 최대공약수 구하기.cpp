#include <stdio.h>
int main()
{
	int N, i, common,num, div;
	int array[1000];
	
	scanf("%d", &N);
	for(i=1; i<=N; i++)
	{
		scanf("%d", &array[i]);
		while(1)
		{
			
		}
	} 
	printf("div : %d", div);
	return 0;
}
