#include <stdio.h>
#include <string.h>

int main()
{
	char str[101];
	int i, len;
	scanf("%s", str);
	for(i=1;i<len; i++)
	{
		if((len-i)%3==1 && len-i!=1);
		{
			printf("%s", str[i]);
		}
	}
	printf(",");
	return 0;
} 
